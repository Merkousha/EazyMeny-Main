@{
    ViewData["Title"] = "مدیریت منو";
}

<div class="container-fluid">
    <div class="row mb-4">
        <div class="col-12">
            <h2><i class="bi bi-card-list me-2"></i>مدیریت منو و محصولات</h2>
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a asp-action="Index" asp-controller="Home" asp-area="">خانه</a></li>
                    <li class="breadcrumb-item active">مدیریت منو</li>
                </ol>
            </nav>
        </div>
    </div>

    <div class="row mb-3">
        <div class="col-12">
            <a asp-action="Create" asp-controller="Product" asp-area="Restaurant"  class="btn btn-primary">
                <i class="bi bi-plus-circle me-2"></i>
                افزودن محصول جدید
            </a>
            <a href="@Url.Action("Create", "Product", new { area = "Restaurant" })" class="btn btn-primary">Create Product</a>
        </div>
    </div>

    <div class="row">
        <div class="col-12">
            <div class="card shadow-sm">
                <div class="card-header bg-light">
                    <h5 class="mb-0">لیست محصولات</h5>
                </div>
                <div class="card-body">
                    <div class="alert alert-info">
                        <i class="bi bi-info-circle me-2"></i>
                        <strong>توجه:</strong> این صفحه در حال توسعه است. به زودی امکان مدیریت کامل محصولات اضافه می‌شود.
                    </div>

                    <div class="table-responsive">
                        <table class="table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th>تصویر</th>
                                    <th>نام محصول</th>
                                    <th>دسته‌بندی</th>
                                    <th>قیمت</th>
                                    <th>وضعیت</th>
                                    <th>عملیات</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr>
                                    <td colspan="6" class="text-center text-muted py-4">
                                        <i class="bi bi-inbox fs-1 d-block mb-2"></i>
                                        هیچ محصولی یافت نشد. برای شروع، محصول جدید اضافه کنید.
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
